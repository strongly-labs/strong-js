{
  "name": "@strongly/cli",
  "version": "0.0.1",
  "description": "Strongly CLI",
  "author": "Strongly Pty Ltd",
  "main": "dist/index.js",
  "bin": {
    "st": "./bin/cli.js"
  },
  "scripts": {
    "start": "tsdx watch --tsconfig tsconfig.build.json --verbose --noClean",
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "test": "tsdx test",
    "lint": "tsdx lint",
    "prepublish": "npm run build"
  },
  "files": [
    "README.md",
    "dist"
  ],
  "dependencies": {
    "fs-extra": "^10.0.0",
    "tslib": "^2.3.0",
    "yargs": "^17.0.1"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.12",
    "@types/yargs": "^17.0.2"
  }
}
