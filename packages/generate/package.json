{
  "name": "@strongly/generate",
  "version": "0.0.1",
  "description": "Strongly Codegen",
  "author": "Strongly Pty Ltd",
  "main": "dist/index.js",
  "bin": {
    "prisma-strongly-generator": "./bin/cli.js"
  },
  "scripts": {
    "start": "tsdx watch --tsconfig tsconfig.build.json --verbose --noClean",
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "test": "tsdx test",
    "prepublish": "npm run build"
  },
  "files": [
    "README.md",
    "dist"
  ],
  "dependencies": {
    "@prisma/generator-helper": "^2.27.0",
    "@prisma/sdk": "^2.27.0",
    "superjson": "^1.7.4",
    "tslib": "^2.3.0"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "devDependencies": {
    "@types/rimraf": "^3.0.1",
    "rimraf": "^3.0.2"
  }
}
