{
  "name": "@strongly/generate",
  "version": "0.0.1",
  "description": "Strongly Prisma Generators",
  "author": "Strongly Pty Ltd",
  "bin": {
    "prisma-strongly-generator": "./bin/cli.js"
  },
  "scripts": {
    "start": "tsdx watch --tsconfig tsconfig.build.json --verbose --noClean",
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "test": "tsdx test",
    "lint": "tsdx lint",
    "prepublish": "npm run build"
  },
  "module": "dist/generate.esm.js",
  "files": [
    "README.md",
    "dist"
  ],
  "dependencies": {
    "@prisma/generator-helper": "^2.27.0",
    "@prisma/sdk": "^2.27.0",
    "superjson": "^1.7.4",
    "tslib": "^2.0.0"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "devDependencies": {
    "@types/rimraf": "^3.0.1",
    "rimraf": "^3.0.2"
  }
}
